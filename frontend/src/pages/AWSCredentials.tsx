import { Typography, Box } from '@mui/material'

export default function AWSCredentials() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        AWS Credentials
      </Typography>
      <Typography variant="body1">
        AWS credentials management interface coming soon...
      </Typography>
    </Box>
  )
}
